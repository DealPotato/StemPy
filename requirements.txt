# Reference requirements for manual development setup. Use setup_runtime.py on
# Windows so torch and torchaudio come from the correct CUDA package index.

PySide6>=6.8,<7
# torch and torchaudio are installed by setup_runtime.py (CUDA 12.4 on NVIDIA)
demucs==4.0.1
audio-separator[gpu]==0.44.2
